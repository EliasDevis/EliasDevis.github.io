name: build-sass
run-name: Build sass files to scss
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v3
        with:
          node-version: '14'
      - run: npm install -g sass
      - run: npm run build:sass